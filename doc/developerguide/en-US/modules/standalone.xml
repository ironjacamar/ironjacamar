<?xml version="1.0" encoding="UTF-8"?>
<chapter id="standalone">
  <title>Standalone</title>

  <section id="standaloneoverview">
    <title>Overview</title>
    
    <para>The standalone IronJacamar container implements Chapter 3 Section 5 of the 
      JCA 1.6 specification which defines a standalone JCA environment.</para>

    <para>The standalone container has the following layout:</para>

    <itemizedlist>
      <listitem>
        <code>$IRON_JACAMAR_HOME/bin/</code>
        <para>contains the run scripts and the SJC kernel.</para>
      </listitem>
      <listitem>
        <code>$IRON_JACAMAR_HOME/config/</code>
        <para>contains the configuration of the container.</para>
      </listitem>
      <listitem>
        <code>$IRON_JACAMAR_HOME/deploy/</code>
        <para>contains the user deployments.</para>
      </listitem>
      <listitem>
        <code>$IRON_JACAMAR_HOME/doc/</code>
        <para>contains the documentation.</para>
      </listitem>
      <listitem>
        <code>$IRON_JACAMAR_HOME/lib/</code>
        <para>contains all the libraries used by the container.</para>
      </listitem>
      <listitem>
        <code>$IRON_JACAMAR_HOME/log/</code>
        <para>contains the log files.</para>
      </listitem>
      <listitem>
        <code>$IRON_JACAMAR_HOME/system/</code>
        <para>contains system deployments files.</para>
      </listitem>
      <listitem>
        <code>$IRON_JACAMAR_HOME/tmp/</code>
        <para>contains temporary files.</para>
      </listitem>
    </itemizedlist>
    
    <para>To start the container execute the following</para>
    <programlisting>
cd $IRON_JACAMAR_HOME/bin
./run.sh
      </programlisting>
      <para>.</para>

  </section>

  <section id="jcasjc">
    <title>IronJacamar/SJC</title>
    <warning>
      This standalone configuration is for development purposes only.
    </warning>

    <para>The IronJacamar/SJC uses the Fungal kernel for its run-time environment.</para>

    <para>The homepage for the Fungal is <ulink url="http://jesperpedersen.github.com/fungal">http://jesperpedersen.github.com/fungal</ulink></para>.

    <para>SJC is short for "Simple JCA Container".</para>
  </section>

</chapter>
